apply plugin: "war"

bootWar {
    mainClass = "com.nicogbdev.gymkanapp.GymkanappApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
